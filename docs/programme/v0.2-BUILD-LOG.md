# v0.2 Build Log — canonical record of what was built, how, and from what

Started 27 August 2026. Maintained by the orchestrator during the v0.2 build; the single source of truth for final technical docs, READMEs, changelogs and attribution. Companion to [`BRIEF.md`](BRIEF.md), [`v0.1-REVIEW.md`](v0.1-REVIEW.md), [`PAID-TOOLS.md`](PAID-TOOLS.md).

Status legend: ✅ delivered and verified · 🔄 in progress · ⏳ queued.

## 1. What v0.2 does (capability register)

### 1.1 Engine — `@opace/content-integrity-core` (TypeScript, MIT, local-only)

| Capability | Detail | Version stamp | Status |
|---|---|---|---|
| Invisible-character detection | 38 carrier rules covering **415 code points**: full Cf format set (ZWSP/ZWNJ/ZWJ/LRM/RLM, bidi embeddings/isolates, word joiner + invisible operators U+2060–2064, BOM, U+061C, U+070F, U+0890–0891, U+08E2, U+180B–180F incl. Mongolian vowel separator, interlinear U+FFF9–FFFB, U+110BD/110CD, **tag block U+E0001 + U+E0020–E007F**), variation selectors U+FE00–FE0F **and** U+E0100–E01EF, CGJ U+034F, Zs space family (NBSP, U+1680, U+2000–200A, NNBSP U+202F, U+205F, U+3000), line/para separators U+2028/9, U+FFFD, unpaired surrogates. Context intelligence: ZWJ inside emoji sequences and ZWNJ/ZWJ in cursive/Indic scripts are exempted; variation selectors after emoji bases skipped, after Han/Mongolian downgraded to note; French-typography spaces stay note-severity. Data-driven tables in `packages/core/src/unicode/data.ts`. | `unicode:2026.08.2` (exported `UNICODE_RULES_VERSION`) | ✅ |
| Homoglyph detection | 60 confusables: 37 Cyrillic + 23 Greek Latin-lookalikes (incl. і, к, ѕ, ј, ԁ, ο, ν, lunate sigma, capital sets). Mixed-script token gate — pure Russian/Greek text never flags. | same | ✅ |
| Writing-signal rules | **51 weighted categories** in `packages/core/src/patterns/en-signals-v2{,-data}.ts`: 46 adapted from avoid-ai-writing (tier 1/2/3 vocabulary, chatbot artefacts, cutoff disclaimers, sycophantic/formulaic openers, "let's" constructions, vague attribution, significance/novelty inflation, hollow intensifiers, emotional flatline, false concessions, template phrases, smart-punct signature, punctuation distribution, function-word trigram entropy, cross-paragraph burstiness, low type-token ratio, AI placeholders, chatbot citation-markup leaks, AI URL params, …) + 5 Opace-original structural rules from owner observations: `signals.em_dash_density` (>6/1000 words and ≥3), `signals.not_just_contrast` ("isn't just X — it's Y"), `signals.uniform_sections` (≥4 sections, CV<0.15; high severity at ≥8/CV<0.10), `signals.uniform_list_items`, `signals.sentence_flatline` (CV<0.25). Normalisation pre-pass (zero-width strip, homoglyph swap) with offset map so spans address the original text. One upstream bug fixed in port (`\b` after `!` in great-question patterns → `\B`). | `en-signals:2026.08.2` (exported `EN_SIGNALS_PATTERN_VERSION`) | ✅ |
| Editorial signals score | `computeEditorialSignals(text)` → 0–100 log-normalised score, trinary classification (`human_like`/`mixed_signals`/`ai_like`), probabilities, confidence, categories hit, status (`scored`/`empty`/`too_short`/`too_long`). False-negative-biased by design (BRIEF §5). Also embedded in `inspect()` results as a `{type:"editorial_signals"}` evidence record on the `style.patterns` method; method status becomes `attention` when classification ≠ human_like. | same | ✅ |
| Protected content | 12 span kinds: currency, number, date, time, unit, url, email, quote, code + **new: name (honorific-led + capitalised-run heuristics, sentence-start rejection, stoplist), organisation (suffix rules Ltd/LLC/plc/… + 2–6-capital acronyms with GMT/EUR/API-style stoplist), citation ((Author et al., 2020) family + adjacent-year et al.)**. Precision-first; same envelope/dedup/sort as before. | — | ✅ |
| Honest statuses | Zero-finding pattern runs carry `{type:"scope_note", rules_run:54}` ("not evidence of human authorship"); unsupported methods say "Not yet available in this release."; provider renamed "Opace writing-signal rules". | — | ✅ |
| Known limits (recorded honestly) | Clean, well-prompted AI prose (the GPT-5.6 article) scores 6/`human_like`/high-confidence — rule tiers cannot catch it; awaits Tier C model (BRIEF §21). Carrier inserted mid-entity can defeat name/org extraction (regex kinds like URL match through). Classic-cliché text reaches `mixed_signals` (32), not `ai_like`. | — | ✅ documented in battery |

### 1.2 Delivery — website checker (first surface)

- Vendored tarballs rebuilt and installed at `opace-website/astro-latest/vendor/content-integrity/` (npm lockfile refreshed; stale-cache issue resolved by explicit tarball install). Site smoke test: slop text scores 44/`ai_like`/high through the installed package. ✅
- Checker UI overhaul (Workstream D): ✅ delivered — SVG score gauge (0–100) + classification labels + confidence/rule-count/version chips + mandatory framing line ("Editorial style evidence from 54 named rules, not proof of authorship. No detector was called."); no-assessment dial for too-short/empty ("A missing score is not a human verdict"); two-rail "Draft evidence" view with severity-tinted span highlighting, visible ⟨ZWSP⟩-style chips, protected-span underlines, click-through to grouped evidence rail (9 categories) and back; rule-match frequency panel (labelled "rule matches, not corpus frequencies"); stubs rendered as dashed "Coming soon — never counted" rows; summary counts runnable checks only; protected content reported `inconclusive`, never pass. Worker watchdog added in core-adapter (4s fallback to main thread; fixed 2 pre-existing type errors). Agent verification: tsc clean for owned files, `astro build` 697 pages OK, Playwright 22/22 assertions (slop ⇒ ai_like 63 with 11 marks; human control ⇒ human_like; 375/320px zero overflow). Framing line uses a comma not an em dash (UI copy bans em dashes).
- Orchestrator final verification (production preview build, scripted Chrome): **10/11 pass; the 1 fail was a test-string bug (plural "3 need review")** — carriers/protected/highlighting all correct on screenshot evidence. Uncovered one real engine defect: classification could contradict its own probabilities (normalization-flag hard override ⇒ "Strong AI-style signals" at score 9 with 68.6% human probability); sent back to Workstream B with required argmax-consistency fix + regression tests. 🔄 fix in progress; re-verification + push blocked on it.
- Cross-surface parity: website's installed engine proven byte-identical to source on findings/methods/signals/versions (battery `cross-surface.test.mjs`). ✅

### 1.3 Test evidence (verbatim totals, 27 Aug 2026)

- Core unit: **41 pass / 0 fail** (was 22 in v0.1). Root suite: typecheck clean; contracts 13 schemas; Python + PHP fixture suites pass. G2 core gate: **24/24**.
- Battery (`npm run test:battery` in `implementation/`): **84/84** — carriers 63, slop 5, uniformity 3, protected 4, cross-surface 9. Every v0.1-review fixture-B carrier now detected; fixture D (modern slop): 11 findings, score 63, `ai_like` high (v0.1: zero findings); human control: 0 findings, `human_like`; perf: 50k chars ≈ 12ms unicode, ≈ 22ms patterns (gates 200/300ms).
- Standing rule (battery README): every new capability lands with a battery extension.

## 2. Attribution and licences (for READMEs/NOTICES)

Recorded in `implementation/THIRD_PARTY_NOTICES.md` (rows appended this build):

| Source | Licence | What was used | Where |
|---|---|---|---|
| avoid-ai-writing — Conor Bronsdon and contributors | MIT | Writing-pattern rules, stylometric methods, ISSUE_WEIGHTS, scoring/classifier logic **adapted to TypeScript**; confusable table data | `en-signals-v2*.ts`, `unicode/data.ts` |
| watermarks-remover — Guillaume Meyer | MIT | Carrier/confusable **table data adapted**; no upstream code distributed | `unicode/data.ts` |
| Pangram Labs technical report (arXiv:2402.14873) | published research | Hard-negative-mining training method informs the planned Tier C classifier (not yet implemented) | plan only |
| Unicode Consortium data (Cf/Zs categories, confusables) | Unicode licence (data) | Category-derived carrier inventory | `unicode/data.ts` |
| Runtime deps unchanged | see NOTICES | ajv, canonicalize, entities, jsonschema, rfc8785, opis/* | — |

Owner-supplied sources: seven internal writing-guideline documents mined into `research/OWNER-DOCS-TELLS.md` (owner IP; note in doc that `HUMAN_WRITING_TECHNIQUES.md` was read from its live copy in `opace-website/astro-latest/docs/articles/`, not the Trash path).

## 3. Research pipeline (feeds `en-signals-v3`)

| Stream | Output | Status |
|---|---|---|
| Web sweep workflow (6 angles: academic, community/Wikipedia catalogue, GitHub tools, industry lists, structural tells, model-specific fingerprints → synthesis) | `research/AI-TELLS-MEGA-PACK.md` + `research/ai-tells-pack-seed.json` (`tells-seed:2026.08.1`): 259 raw → **114 merged tells** — tier A 19 / B 39 / C 56; kinds: phrase-regex 40, structural 31, stylometric 16, lexical 13, model-pack 9, punctuation 5. Novelty verdict (orchestrator): genuinely new — (1) **artefact-forensics category** (exposed citation tokens oaicite/citeturn/【N†L】, utm_source=chatgpt.com params, PUA/math-bold leakage, placeholders, reasoning leaks) — near-zero FP, model-attributing, promote to front of v3 as Tier A deterministic evidence; (2) **era-decay/attribution-flip architecture** (delve dead 2025; em dash now Claude-attributing post-Nov-2025 GPT suppression); (3) human-whitelist negative signals; (4) participial significance tails + copula-avoidance ratio; corrections: hedging direction REVERSED vs folklore, uniform-FAQ tells must stay corroboration-only (SEO templates legitimately uniform), never stylometrics alone (non-native FP landmine). Licensing mapped: Apache-2.0/MIT lists bundleable with credit; AGPL gptslop reimplement-only; Wikipedia CC BY-SA paraphrase-only; vendor lists cite-only. Researcher-6 tangents lost to truncation (noted in MD). | ✅ |
| Owner-docs miner | `research/OWNER-DOCS-TELLS.md` — ~23 new lexical tells, ~32 new phrases with regexes, 11 structural rules (setup-and-expansion cadence, power-verb+buzz-adjective compound), 5 stylometric measures (passive ratio, specificity score, adjacent-sentence lemma repetition); NEW-vs-covered table; tier merge plan | ✅ |
| SynthID & Originality deep dive | ✅ `research/SYNTHID-AND-ORIGINALITY-DEEP-DIVE.md`. Headlines: (1) Originality's SynthID "tool" is an animated simulation — no real model/keys/user input/wrong-key/attack demos; we hold the actual Apache-2.0 reference and the core maths (LCG hash → Bernoulli g-values → tournament reweighting → mean-score detection, ~200 lines) can run genuinely in-browser, with a GPT-2 generation runner via the Python loopback engine — wrong-key collapse demo is the flagship interaction their format cannot copy; (2) their WP plugin confirmed closed on WordPress.org (2 July 2026, author request) — empty slot with residual search demand for ours; (3) their study formula decoded: model release → templated 5,000-sample study in 5–9 days, "99%+ at AI Allowance 15%", dense interlinks, zero FPR reporting, no reproducible corpus — Opace replicates cadence but publishes FPR + corpus version + reproduction command; (4) Claude models since ~2 Aug 2026 embed text watermarks (EU AI Act Art. 50) with no public detector yet — highest-intent lowest-competition content query in the space; boundary copy per BRIEF §5 is both compliant and differentiating; (5) SynthID detection needs only tokeniser + keys + hashing (no model): browser can score any text live; spec details recorded (ngram_len=5, per-layer keys, mean g ≈ 0.75 vs 0.5 null, ~95% TPR @1% FPR near 200 tokens, weak on low-entropy text, degraded by paraphrase/translation). | ✅ |
| Merge pass (v3) | Combine research streams into versioned `en-signals-v3` data + battery extensions. Novelty verdict delivered 27 Aug (see web-sweep row): artefact forensics promoted to front of queue. | ⏳ |

## 4. Integration changes made by the orchestrator (not by agents)

- `packages/core/src/index.ts`: re-exported `inspectSignalsV2`, `computeEditorialSignals`, `EN_SIGNALS_PATTERN_VERSION`, `UNICODE_RULES_VERSION`.
- `packages/core/src/inspect.ts`: version constants wired (unicode + signals); `rules_run` 54; editorial-signals evidence record added to `style.patterns`; attention status on non-human classification.
- Rebuild + `npm pack` of contracts/core/browser; site lockfile refresh via explicit tarball install.

## 5. Release state — DEPLOYED AND VERIFIED LIVE (27 August 2026, evening)

- Commits to `opace-latest` `main`: `8e57a082` (checker v0.2), `59c40c62` (NPM_VERSION 11.3.0 pin), `1fd45afe` (repacked tarballs + clean lockfile — the actual deploy fix). Netlify deploy succeeded; **live at https://opace.agency/tools/ai/content-verification-integrity/checker/**.
- Deploy-failure chronicle (for the docs): two failed Netlify builds were caused by the rebuilt vendored tarballs having been packed with monorepo `file:../` dependency specs; fresh installs wrote dangling `link: true` entries with empty target nodes into package-lock.json, crashing npm ("Cannot destructure property 'package' of 'node.target'"). Fix: pack-time rewrite of internal deps to version specs + lockfile regeneration; a permanent `pack:vendor` script is queued as a follow-up task. The NPM_VERSION pin stays as build hygiene.
- **Live functional verification (scripted Chrome against production, 6/6)**: slop fixture ⇒ "Strong AI-style signals" 63/100 with 14 highlighted spans and honest framing; human control ⇒ "Reads human-like"; carrier text ⇒ needs-review with protected names extracted and no false strong-AI verdict. Screenshots: scratchpad `live-slop.png`, `live-human.png`, `live-carriers.png`.

- **Post-launch owner finding (27 Aug, evening): over-claiming presentation on clean results.** A GPT-3-era case study and the GPT-5.6 article both scored 1–2/100 and rendered "Reads human-like · HIGH CONFIDENCE" — a positive human verdict the rule tier cannot support (it only knows no style tells matched). Fix dispatched to the UI workstream: human_like relabelled "No strong AI-style signals", confidence chip replaced with "STYLE RULES ONLY" for clean results, neutral (non-green) gauge tone, and an always-visible callout explaining that carefully prompted AI text often carries no style tells and that the trained local-model check is in development. This is the correct honest ceiling until Tier C ships; the three greyed checks are correctly absent (Anthropic verifier does not exist publicly anywhere; Named local signals = the Phase-2 trained-model tier; provenance requires file inputs + c2pa-js, Phase 2).

- **Honest-presentation fix deployed and verified live** (`7a0f7f1c`): clean results now read "No strong AI-style signals" with a neutral STYLE RULES ONLY chip, grey gauge, and (≥120 engine-words) an info callout explaining clean style evidence ≠ human authorship. Live checks confirmed new label, chip, and removal of "Reads human-like"; callout threshold behaves to spec.
- **SynthID-Text real detector shipped** (`packages/watermark-lab` 0.1.0, MIT, ~1.69MB browser bundle): faithful TS port of the DeepMind reference detection path (Apache-2.0, credited) — exact g-value/mask parity with the reference, 72 fixture×key scores agree <1e-4; GPT-2 BPE tokeniser with 12/12 Python-parity; **real watermarked fixtures generated with torch + the reference tournament sampler** (GPT-2 124M, 3 named demo keys, 6 layers — documented deviation from the 30-layer example because 30 dilutes meanG to ~0.52; fixtures score 0.640–0.694 right-key vs ~0.5 wrong-key/unwatermarked, truncation/substitution variants included, seed-selection rule documented in the manifest). 30/30 tests, 10k tokens scored in 50ms. Every surfaced string carries the demo-key / no-Claude-claims boundary (BRIEF §5). Vendored into the site (`opace-watermark-lab-0.1.0.tgz`, lockfile stays link-free). Lab UI workstream in progress on the readiness-lab page: fixture explorer with per-token g-value colouring, the wrong-key collapse toggle, paste-your-own honest scoring, degradation table, honesty rail.

## 5a. v0.3 capability release — DEPLOYED AND VERIFIED LIVE (27 August 2026, late evening)

- Site commit `38151460` (after `7a0f7f1c` honesty fix): **en-signals:2026.08.3** vendored (103 categories — 52 new incl. 7-rule artefact forensics with 11 attributed citation-token patterns, era/attribution metadata on all rules, stylometric weight cap, SEO-template + non-native FP guards, 67 documented exclusions); **live C2PA provenance** (official @contentauth/c2pa-web 0.14.3, images+PDF, local-only, honest status mapping, trust-list checking disabled and disclosed); **SynthID-Text interactive lab** on the readiness-lab page (real reference mathematics, fixture explorer with per-token g-values, wrong-key collapse, paste scoring with honest nulls, degradation table; engine lazy-loaded ~1.6MB chunk).
- Evidence: core 56/56, battery 96/96, lab UI 24/24, provenance 18/18, combined production-preview 6/6, **live production 6/6**. Fixture D still 63/ai_like; human control clean; perf 27–31ms at 50k.
- Docs canon complete: implementation README.md (master), docs/CAPABILITIES.md (exhaustive register), DESCRIPTIONS.md (copy-ready WP/Chrome/npm/Astro listing text on the claims ladder), package READMEs — all placeholders patched with final counts (103 categories/106 named rules).
- Implementation repo: **first commit `9bd32de`** containing everything. GitHub repo creation is the one owner action outstanding: the stored git credential is scoped to opace-latest only (org repo-create returned 404; no further scope probing). Command for the owner: `gh auth login` then `gh repo create OpaceDigitalAgency/opace-ai-content-integrity --private --source implementation/ --push` (flip public when ready).
- Backlog notes: pre-existing site-wide GTM `form_start` beacon fires on form interaction (no file/text content; privacy-page mention recommended); pack:vendor permanent script chip pending (task_641ad659).
- Next in flight: real-world AI-sample evaluation campaign (owner instruction): collect labelled AI outputs from multiple models across the web + human controls, score with the engine, refine calibration within honest bounds, document in research/REAL-WORLD-EVAL-2026-08.md.

## 5b. Real-world evaluation and en-signals:2026.08.4 — DEPLOYED AND VERIFIED LIVE (27 August 2026, night)

- **Evaluation** (`research/REAL-WORLD-EVAL-2026-08.md`): 37 web-collected samples (30 labelled AI across ChatGPT/GPT-5/Claude/Gemini/Grok/DeepSeek/wild-slop, 4 verified human controls incl. pre-2022 and non-native writing). Baseline result at 2026.08.3: **0/30 ai_like, 1/30 mixed** — evidence present (7/7 artefact-bearing samples had artefact hits, several scored above every human) but classification over-conservative; **0/4 human false positives**; the 23 clean-prose samples are the documented model-tier ceiling.
- **Refinement** (site commit `052c0c51`, engine `en-signals:2026.08.4`): five evidence-based escalation policies from the eval analyst (artefact floor, citation co-occurrence → ai_like, artefact+score, formatting cluster, finding breadth), implemented as raise-only escalations over the argmax base with an explanatory `escalation` result field; do-not-do list respected (no weight/threshold changes that touch humans). **After: 3/30 ai_like + 3/30 mixed (slop-class 6/7 escalated; the arrow-only sample deliberately held per analyst caution); still 0/4 human escalations.** Core 63/63, battery 96/96, root suite green, perf 27ms/50k.
- **Live verification**: citation-token+utm sample renders "Strong AI-style signals" on the production checker with the 2026.08.4 version chip. Docs (README/CAPABILITIES/core README) updated to 2026.08.4 with the escalation-policy note; implementation repo commits `9bd32de` (initial import) + `34dc0ee` (escalation + eval report).
- **Honest verdict against the brief**: within the rule tier's scope the engine is now accurate and consistent — it catches slop-class and artefact-bearing AI at zero measured human false-positive cost, and says "no strong style signals, not proof of human" on clean prose rather than guessing. Detecting clean prose remains the trained-model tier (top of roadmap), as every document states.

## 5c. Website-complete release — DEPLOYED AND VERIFIED LIVE (28 August 2026)

- Site commit `79b3ac5b`; implementation commit `10c7b92`. Live-verified: 113-rule counts on checker, Named local signals consent flow, provenance live wording, methodology "Published results and evidence" section, model file served (34.3MB, HTTP 200), thresholds v1.1 @ 0.857.
- **Named local signals (beta) shipped**: e5-small cycle-1 classifier, per-channel int8 ONNX (near-lossless, mean drift 0.0095 vs fp32 — the original per-tensor int8 was measured badly degraded, mean drift 0.22/23 verdict flips, and retired); threshold 0.857 = **0/116 human false positives** (zero business-marketing FPs), costing detection: 2/23 clean-prose, 2/30 all-AI. Disclosure line on every result states this plainly; cycle-2 plan (marketing hard negatives + wiki-genre AI mirrors) recorded. Golden parity 5/5 live-model.
- **Rhythm tier** (`en-signals:2026.08.5`, 113 rules/110 categories): 0/44 humans firing, corpus-backed calibration.
- **40-sample verified human corpus** shipped with provenance manifest.
- Orchestration note: the reconciliation agent's script ran single-threaded (38+ min); orchestrator killed it and re-ran lean in 7.3s — numbers agreed with the agent's own definitive rerun; agent correctly quarantined unexpected files as untrusted (defence-in-depth behaving as designed).
- Final combined stack on the quarantined real-world set: **8/30 AI flagged (rules or model) vs 1/30 at baseline; 0 human false positives at every layer** (0/116 model, 0/44 rhythm, 0/4 + 40/40 clean on engine).

## 5d. Per-provider calibration release `en-signals:2026.08.6` — DEPLOYED AND VERIFIED LIVE (28 August 2026, afternoon)

- Site commit `a535ea4a`, live-verified post-CDN: markdown-style AI text → "Strong AI-style signals"; watermark known-key scan runs on every assessment (per-key table, real signal detection on lab samples); version chip 2026.08.6; human control clean.
- **Engine v6** (from the 1,896-sample per-provider evaluation): markdown-furniture gate, formatting escalation floor, bold/heading-density rules, breadth relaxed to ≥6/≥4, surrogate-pair crash fix. **Measured rules-only detection at 0/169 human FPs: all-AI 66.7% (1,152/1,727); per-slice 53–95% on every 2025-26 provider (Claude 53.3, Grok 68, OpenAI 71, Meta 87, Gemini 89, Mistral 93, DeepSeek 95); 2024-25: OpenAI 80.5, Gemini 82.7, Meta 71.3, Claude 16; conversational 2022 Q&A 0% (register, not age — cliché-free by measurement).** Caveat recorded: furniture-driven share weakens on stripped plain-paste; register-robust subset ~22–33%.
- **Owner's 50%+ per-provider bar: met on every current-era (2025-26) provider by rules alone.**
- **Model-tier temperature investigation (orchestrator-direct, agents stalled repeatedly on infra):** temperature is monotonic → cannot change ranking; direct measurement of the operating menu: 0 FP/169 → 0.1% TPR; ~3% FPR → 54%; ~4% → 59–93%. Cycle-1 separation is insufficient at the zero-FP wall; the earlier "0.80 sweet spot" costs 3–4% human FPs and is rejected on principle. **Shipped zero-FP threshold stands; cycle-2 retraining (biz hard negatives + wiki AI mirrors, manifest in provider-eval report) is the only honest path for the model tier.**
- Agent-stream instability note: three subagents stalled repeatedly (watchdog); orchestrator completed their verification/measurement work directly. Outstanding from those streams: T2B Tier-2 browser deployment (offline foundation + spec + golden vectors exist; its cycle-1 measured profile ~30% clean-prose at ~7% FPR faces the same zero-FP wall — bundled into cycle-2 scope), PP era-matrix 2023-24 extension (RAID; measurement-only, next session).

### Pending gates (next release)

- Nothing committed or pushed yet. Publication target: `github.com/OpaceDigitalAgency/opace-latest` (push triggers Netlify build → live at opace.agency). Owner authorised the push in this session once verification passes.
- Orchestrator's remaining gates before push: review Workstream D output; enforce honest confidence copy for the clean-AI `human_like` case (battery finding #1); in-browser verification of the checker (fixtures + the owner's GPT-5.6 article + human control + carrier text) with screenshots; commit with tidy message; push; verify live URL post-deploy.
- Out of scope this release (unchanged from plan): Tier C trained model, C2PA, local detector engine wiring, WordPress/Chrome/Astro sync (next release, from the same tarballs), benchmark corpus.

## 6. Notes for future README writers

- Never present the signals score as authorship detection — BRIEF §5/§21 wording is binding ("editorial style evidence from 54 named rules; not proof of authorship; no detector was called").
- The engine is one artefact used identically on every surface; cite `EN_SIGNALS_PATTERN_VERSION`/`UNICODE_RULES_VERSION` in receipts/changelogs.
- Credit avoid-ai-writing and watermarks-remover prominently (both MIT) — the reuse-first rule in BRIEF §4 is now binding policy.
- The battery README documents what each suite proves; the known-limits row above is the honest-limitations section of any future README.
