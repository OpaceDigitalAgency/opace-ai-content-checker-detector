# Architecture consolidation follow-up register

**Created:** 31 August 2026  
**Purpose:** discrepancies found while consolidating the architecture, science and evidence record. These are deliberately separate from the architecture document so that unresolved record drift does not become product truth.  
**Rule:** verify each item against the live system and current repository before editing it. Active data-lane files must not be changed until their owning process has finished.

## Priority register

| ID | Priority | Discrepancy | Evidence observed | Investigation or correction needed |
|---|---|---|---|---|
| **F01** | P0 | Revision-specific Cloud Run safety proof is stale | `CLOUD-RUN-SAFETY-REVERIFICATION-2026-08-31.md` proves the kill switch and ten-path zero-body-logging drill on `opace-detector-00027-yuq`. At the architecture audit, Cloud Run routed 100% to `opace-detector-00008-wsf`, created 31 August at 09:41:54Z with a different image digest. | Re-run the controlled kill-switch delivery and ten-path marker search on the current serving revision. Confirm restore exactness and log exclusions. Do not copy the earlier PASS forward. |
| **F02** | P0 | `docs/RELEASE-STATE.md` presents an obsolete current state | Its opening correction calls `00005-284` live and its main register is dated 29 August. Later sections contain newer notes, but old rows still say the GitHub repository, Cloud Run and research publication are not live. | Rebuild the register from current GitHub, website, registry and Cloud Run observations. Keep history in a clearly retired section. Remove internal contradictions rather than adding another superseding paragraph. |
| **F03** | P0 | Root `STATUS.md` is internally contradictory and materially stale | The file still says the public GitHub repository does not exist, hosted inference is held, there is no endpoint, and 0.984 opening-only figures ship. A later appended authorisation note says the repository, Cloud Run and research publication happened. | Replace the stale current-state blocks with the 31 August release boundary. Link to the model manifest and task board. Retain the former state as dated history only if needed. |
| **F04** | P0 | `docs/CAPABILITIES.md` headline and service revision are retired | Status is 29 August. The introduction gives 90.3% detection and 1.34% human false positives and a service revision `00005-284`, while the deployed manifest gives current server 95.8%/0.97%, browser 96.4%/1.94%, `segments-v3` and the `0.9855/0.9763` pair. | Refreeze the capability register from the deployed manifest and current service. Every historical table must name its retired threshold and segmentation. |
| **F05** | P1 | WordPress version sources disagree | `wordpress/opace-ai-content-integrity/opace-ai-content-integrity.php` and `readme.txt` say 1.0.7. The same component's `package.json` says 1.0.6. | Determine whether package metadata is part of the release identity. If yes, bump it and rebuild all candidate evidence. If no, document why and add a test that enforces the intended identities. |
| **F06** | P1 | Cycle-5 artefacts changed during audit and cannot yet support a stable report | `dataset-manifest.json` changed from 31,473 to 31,800 rows during the audit, its matched input changed to `frozen-matched.snapshot.jsonl`, the live generated file kept growing, and the early `train-report-full.json` with only 400 train and 200 calibration rows was removed. | Wait for the data lane to declare completion. Then verify frozen input hashes, split totals, quarantine, held-out model family and topic rules, feature file alignment, report denominators and naming. No interim file should be cited as cycle-5 performance. |
| **F07** | P1 | Current service limit change still needs full release proof | The live `/v1/status` reports 100,000 characters, 8,000 words and 197 inferences. The source, deploy script and security note were committed during this audit as `62b8e11`, closing the original source-control gap, but revision-specific safety proof still belongs to `00027-yuq`. | Confirm that the live image is reproducible from `62b8e11`, update the current release records, and close this item together with F01 after the revision-specific controls pass. |
| **F08** | P1 | Current service revision numbering and image lineage need a release note | Cloud Run revision list moves from `00027-yuq` to `00008-wsf`, with a later creation time and different digest. Cloud Run accepts this revision name, but the unusual ordinal makes hand-reading chronology unsafe. | Record creation timestamp and image digest as well as revision name in future deployment notes. Identify the deployment command or suffix choice that produced `00008-wsf`. |
| **F09** | P1 | Old and current display-band names coexist | `thresholds.json` contains technical labels such as `Very likely AI`, `Leaning AI` and `Leaning human`; the live UI maps them to the five owner-approved labels `Likely human` through `Strongly AI`. | Keep the mapping as the source of presentation truth, but update documentation that quotes the JSON labels as if users see them. Add or retain a test preventing raw manifest labels reaching the UI. |
| **F10** | P2 | Model evaluation records can be misread as current production performance | `current-models/CURRENT-MODEL-EVAL.md` uses an older 0.8533 threshold and reports major register-specific failures. It is valuable research but not the deployed cycle-2 paired operating point. | Add an explicit historical-study banner and link to the current manifest. Preserve the results, corpus and threshold unchanged. |
| **F11** | P2 | C2PA references should consistently point to version 2.4 | Some earlier discovery links and records point at 2.2, while the code and capability register implement the 2.4 A.8 unstructured-text wrapper. The official 2.4 specification confirms A.8 and `C2PATXT\0`. | Sweep public and repository links. Use the official 2.4 specification for the current wrapper claim, while retaining older-version links only for dated history. |
| **F12** | P2 | Release artefact evidence is spread across superseded hashes | `RELEASE-STATE.md` records multiple rebuilds and explicitly says earlier package gates do not carry to the rebuilt bytes. Some status prose still reads as if those candidates are submission-ready. | Produce one current candidate manifest per surface with exact hash, version, test evidence and remaining gate. Mark every superseded archive as non-release. |
| **F13** | P0 | The public `README.md` still calls the 5,558-document corpus fully unseen | The opening evidence block says the model had never seen the documents and that every row was hash-quarantined. The deployed manifest's corrected record says 268/922 AI documents occur in a cycle-2 split, including 168 in training; 11/4,636 human documents occur in a split. | Correct every live README occurrence and keep the retired claim only inside an explicit retraction. The new consolidated-reference banner is a temporary routing safeguard, not closure. |
| **F14** | P1 | The README's same-engine claim is too broad for the WordPress PHP path | README says the same compiled engine powers every surface and identical input produces identical findings. `RELEASE-STATE.md` documents a 507-line PHP subset with 3 versus 116 writing rules, 16 versus 38 carrier coverage and a deliberately different namespace. | Narrow the claim to the surfaces and paths that really use the compiled engine. Explain the WordPress quick-check subset and Lab path separately. |
| **F15** | P1 | `docs/EVIDENCE-INDEX.md` repeats corrected or retired claims as current | Examples include `longform-corpus` described as unseen by the model and a hosted-service record pinned to revision `00003-bfq`. The index mixes current, historical and superseded evidence without a uniform state field. | Add state, date, runtime and operating-point columns. Correct the corpus overlap and current service route, while retaining old revisions as dated evidence. The new read-first banner is a routing safeguard only. |

## Evidence commands used for the audit

The following read-only checks produced the observations above:

```text
gcloud run services describe opace-detector --region europe-west1 --project opace-ai-detector
gcloud run revisions list --service opace-detector --region europe-west1 --project opace-ai-detector
curl https://opace-detector-877422072168.europe-west1.run.app/v1/health
curl https://opace-detector-877422072168.europe-west1.run.app/v1/status
git status --short --branch
npm test
npm run test:battery
```

No kill switch was fired and no service configuration was changed during this consolidation.
