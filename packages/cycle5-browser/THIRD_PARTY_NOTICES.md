# Third-party notices

- `onnxruntime-web` 1.29.0 is distributed under the MIT licence.
- The model architecture is based on `intfloat/e5-small`, recorded as MIT in the programme's dependency ledger. The model bytes are not included in this package.

This source package contains no model, vocabulary or WebAssembly binary. A host application must supply an allowlisted asset location and make any download an explicit user action.
