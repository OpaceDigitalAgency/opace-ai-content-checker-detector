import type { AnalysisRequest, AnalysisResult } from "@opacedev/ai-content-checker-contracts";
import { WORKER_PROTOCOL_VERSION,type InspectionPhase,type WorkerRequest,type WorkerResponse } from "./protocol.js";
export interface InspectionWorkerClient {inspect(request:AnalysisRequest,options?:{signal?:AbortSignal;onProgress?:(phase:InspectionPhase)=>void}):Promise<AnalysisResult>;dispose():void}
export function createInspectionWorker(options:{workerUrl?:URL}={}):InspectionWorkerClient{
  if(typeof Worker==="undefined")throw new Error("worker_unsupported");const worker=new Worker(options.workerUrl??new URL("./worker/entry.js",import.meta.url),{type:"module"});const pending=new Map<string,{resolve:(v:AnalysisResult)=>void;reject:(e:Error)=>void;onProgress?:(p:InspectionPhase)=>void}>();
  worker.onmessage=(event:MessageEvent<WorkerResponse>)=>{const m=event.data,p=pending.get(m.id);if(!p||m.protocol_version!==WORKER_PROTOCOL_VERSION)return;if(m.type==="progress")p.onProgress?.(m.phase);else{pending.delete(m.id);m.type==="result"?p.resolve(m.result):p.reject(new Error(m.code));}};
  return {inspect(request,opts={}){const id=`inspect_${Date.now()}_${Math.random().toString(36).slice(2)}`;return new Promise((resolve,reject)=>{pending.set(id,{resolve,reject,onProgress:opts.onProgress});const abort=()=>{worker.postMessage({protocol_version:WORKER_PROTOCOL_VERSION,type:"cancel",id} satisfies WorkerRequest);pending.delete(id);reject(new DOMException("Inspection cancelled","AbortError"));};if(opts.signal?.aborted)return abort();opts.signal?.addEventListener("abort",abort,{once:true});worker.postMessage({protocol_version:WORKER_PROTOCOL_VERSION,type:"inspect",id,request} satisfies WorkerRequest);});},dispose(){for(const p of pending.values())p.reject(new Error("worker_disposed"));pending.clear();worker.terminate();}};
}
