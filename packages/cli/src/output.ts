export type OutputFormat="text"|"json"|"jsonl"|"html";
import canonicalize from "canonicalize";
const escape=(v:unknown)=>String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]!));
export function render(value:any,format:OutputFormat,noColour=true):string{void noColour;if(format==="json"||format==="jsonl"){const encoded=canonicalize(value);if(encoded===undefined)throw new Error("invalid_canonical_json");return encoded+"\n";}if(format==="html"){const methods=(value.methods??[]).map((m:any)=>`<li><strong>${escape(m.status)}</strong> ${escape(m.id)} — ${escape(m.limitations?.join(" ")??"")}</li>`).join("");return `<!doctype html><html lang="en-GB"><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Opace AI Content Integrity evidence</title><style>body{font:16px system-ui;max-width:72rem;margin:auto;padding:2rem}pre{white-space:pre-wrap;overflow-wrap:anywhere}.status{text-transform:uppercase}</style><main><h1>Opace AI Content Integrity evidence</h1><p>Evidence, not guarantees.</p><ul>${methods}</ul><details><summary>Machine record</summary><pre>${escape(JSON.stringify(value,null,2))}</pre></details></main></html>\n`;}const methods=(value.methods??[]).map((m:any)=>`${String(m.status).toUpperCase().padEnd(14)} ${m.id}\n  ${m.limitations?.join(" ")??""}`).join("\n");return `Opace AI Content Integrity 0.1.0\n${methods||JSON.stringify(value,null,2)}\nEvidence, not guarantees.\n`;}
